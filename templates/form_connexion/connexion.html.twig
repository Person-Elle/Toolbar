{% block body %}
    {# templates/form_connexion/connexion.html.twig #}
    {% include 'menu.html.twig' %}

    <h1>Connexion</h1>

    {{ form_start(form) }}

    {{ form_row(form.email, {
      'attr': {
         'class': 'form-control', 
         'placeholder': 'Adresse e-mail'
         }
   }) }}

    {{ form_row(form.password, {
      'attr': {
         'class': 'form-control', 
         'placeholder': 'Mot de passe'
      }
   }) }}
   
    {{ form_end(form) }}
{% endblock %}
